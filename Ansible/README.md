Ansible has stored all necessary modules and YAML template for build a play book.
You can find it by below cmds.
[nazmul@minion ~]$ ansible-doc -l | grep ufw
[nazmul@minion ~]$ ansible-doc community.general.system.ufw
